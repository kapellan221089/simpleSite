{
  "name": "simplesite",
  "version": "1.0.0",
  "author": "kapellan",
  "private": true,
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "@nuxtjs/pwa": "*",
    "express": "*",
    "nuxt": "*",
    "socket.io": "*",
    "vue-socket.io": "*",
    "vuetify": "*",
    "vuetify-loader": "*"
  },
  "devDependencies": {
    "nodemon": "*",
    "stylus": "*",
    "stylus-loader": "*"
  }
}
